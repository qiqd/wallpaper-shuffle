using System.Threading.Tasks;

namespace WallpaperShuffle.test
{
    internal class Test
    {
        public static async Task fn()
        {
            //List<WallpaperSourceItem> items = WallpaperResource.LoadWallpaperResources();
            //string title = Properties.Settings.Default.ResourceTitle;
            //WallpaperSourceItem value = items.Find(t => t.Equals(title));
            //string URL = value.url + "?";
            //string reqeustParams = "";
            //foreach (var item in value.arguments)
            //{
            //    reqeustParams += "&" + item;
            //}

            //HttpClient httpClient = new HttpClient();
            //HttpResponseMessage response = await httpClient.GetAsync(URL + reqeustParams);
            //HttpResponseMessage httpResponseMessage = response.EnsureSuccessStatusCode();
            //httpResponseMessage.ToString();
        }
    }
}